class Input_Botton {

    // 梵語のボタンにイベントを設置する。
    static SanskritBotton(inputId) {
	    // ボタン入力
	    $('#button-a').click( function(){
            // カーソル位置
            var selIdx = $(inputId).get(0).selectionStart;
            // 挿入
            Input_Botton.add_chara($(this).val(), selIdx, inputId);
        });
        $('#button-i').click( function(){
            // カーソル位置
            var selIdx = $(inputId).get(0).selectionStart;
            // 挿入
            Input_Botton.add_chara($(this).val(), selIdx, inputId);
        });
        $('#button-u').click( function(){
            // カーソル位置
            var selIdx = $(inputId).get(0).selectionStart;
            // 挿入
            Input_Botton.add_chara($(this).val(), selIdx, inputId);
        });
        $('#button-r').click( function(){
            // カーソル位置
            var selIdx = $(inputId).get(0).selectionStart;
            // 挿入
            Input_Botton.add_chara($(this).val(), selIdx, inputId);
        }); 
        $('#button-R').click( function(){
            // カーソル位置
            var selIdx = $(inputId).get(0).selectionStart;
            // 挿入
            Input_Botton.add_chara($(this).val(), selIdx, inputId);
        });
        $('#button-l').click( function(){
            // カーソル位置
            var selIdx = $(inputId).get(0).selectionStart;
            // 挿入
            Input_Botton.add_chara($(this).val(), selIdx, inputId);
        });          
        $('#button-L').click( function(){
            // カーソル位置
            var selIdx = $(inputId).get(0).selectionStart;
            // 挿入
            Input_Botton.add_chara($(this).val(), selIdx, inputId);
        });
        $('#button-G').click( function(){
            // カーソル位置
            var selIdx = $(inputId).get(0).selectionStart;
            // 挿入
            Input_Botton.add_chara($(this).val(), selIdx, inputId);
        }); 
        $('#button-J').click( function(){
            // カーソル位置
            var selIdx = $(inputId).get(0).selectionStart;
            // 挿入
            Input_Botton.add_chara($(this).val(), selIdx, inputId);
        });
        $('#button-M').click( function(){
            // カーソル位置
            var selIdx = $(inputId).get(0).selectionStart;
            // 挿入
            Input_Botton.add_chara($(this).val(), selIdx, inputId);
        });
        $('#button-N').click( function(){
            // カーソル位置
            var selIdx = $(inputId).get(0).selectionStart;
            // 挿入
            Input_Botton.add_chara($(this).val(), selIdx, inputId);
        });
        $('#button-t').click( function(){
            // カーソル位置
            var selIdx = $(inputId).get(0).selectionStart;
            // 挿入
            Input_Botton.add_chara($(this).val(), selIdx, inputId);
        });          
        $('#button-d').click( function(){
            // カーソル位置
            var selIdx = $(inputId).get(0).selectionStart;
            // 挿入
            Input_Botton.add_chara($(this).val(), selIdx, inputId);
        });           
        $('#button-z').click( function(){
            // カーソル位置
            var selIdx = $(inputId).get(0).selectionStart;
            // 挿入
            Input_Botton.add_chara($(this).val(), selIdx, inputId);
        });          
        $('#button-s').click( function(){
            // カーソル位置
            var selIdx = $(inputId).get(0).selectionStart;
            // 挿入
            Input_Botton.add_chara($(this).val(), selIdx, inputId);
        });  
    }

    static LatinBotton(inputId){
	    // ボタン入力
	    $('#button-a').click( function(){
            // カーソル位置
            var selIdx = $(inputId).get(0).selectionStart;
            // 挿入
            Input_Botton.add_chara($(this).val(), selIdx, inputId);
        });
        $('#button-i').click( function(){
            // カーソル位置
            var selIdx = $(inputId).get(0).selectionStart;
            // 挿入
            Input_Botton.add_chara($(this).val(), selIdx, inputId);
        });
        $('#button-u').click( function(){
            // カーソル位置
            var selIdx = $(inputId).get(0).selectionStart;
            // 挿入
            Input_Botton.add_chara($(this).val(), selIdx, inputId);
        });
        $('#button-e').click( function(){
            // カーソル位置
            var selIdx = $(inputId).get(0).selectionStart;
            // 挿入
            Input_Botton.add_chara($(this).val(), selIdx, inputId);
        }); 
        $('#button-o').click( function(){
            // カーソル位置
            var selIdx = $(inputId).get(0).selectionStart;
            // 挿入
            Input_Botton.add_chara($(this).val(), selIdx, inputId);
        }); 
    }

    static PolishBotton(inputId){
        // ボタン入力
        $('#button-a').click( function(){
            // カーソル位置
            var selIdx = $(inputId).get(0).selectionStart;
            // 挿入
            Input_Botton.add_chara($(this).val(), selIdx, inputId);
        });
        $('#button-c').click( function(){
            // カーソル位置
            var selIdx = $(inputId).get(0).selectionStart;
            // 挿入
            Input_Botton.add_chara($(this).val(), selIdx, inputId);
        });
        $('#button-e').click( function(){
            // カーソル位置
            var selIdx = $(inputId).get(0).selectionStart;
            // 挿入
            Input_Botton.add_chara($(this).val(), selIdx, inputId);
        }); 
        $('#button-l').click( function(){
            // カーソル位置
            var selIdx = $(inputId).get(0).selectionStart;
            // 挿入
            Input_Botton.add_chara($(this).val(), selIdx, inputId);
        });
        $('#button-n').click( function(){
            // カーソル位置
            var selIdx = $(inputId).get(0).selectionStart;
            // 挿入
            Input_Botton.add_chara($(this).val(), selIdx, inputId);
        });
        $('#button-o').click( function(){
            // カーソル位置
            var selIdx = $(inputId).get(0).selectionStart;
            // 挿入
            Input_Botton.add_chara($(this).val(), selIdx, inputId);
        }); 
        $('#button-s').click( function(){
            // カーソル位置
            var selIdx = $(inputId).get(0).selectionStart;
            // 挿入
            Input_Botton.add_chara($(this).val(), selIdx, inputId);
        });
        $('#button-z1').click( function(){
            // カーソル位置
            var selIdx = $(inputId).get(0).selectionStart;
            // 挿入
            Input_Botton.add_chara($(this).val(), selIdx, inputId);
        });
        $('#button-z2').click( function(){
            // カーソル位置
            var selIdx = $(inputId).get(0).selectionStart;
            // 挿入
            Input_Botton.add_chara($(this).val(), selIdx, inputId);
        });
    }

    // 入力ボックスに文字の挿入
    static add_chara(str, selIdx, inputId)
    {
      // テキストボックスの値を取得
      var text_sentence = $(inputId).val();
      // 入力欄に入れる
      $(inputId).val(Input_Botton.strIns(text_sentence, selIdx, str));
    }

    // 文字列の挿入
    static strIns(str, idx, val){
        // カーソルの位置に文字を入れる
        var res = str.slice(0, idx) + val + str.slice(idx);
        // 結果を返す
        return res;
    };

}